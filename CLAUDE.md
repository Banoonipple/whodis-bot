# Git workflow

When committing changes in this repo:
- Commit to the `sarim-dev` branch, not directly to `main`.
- Open a pull request with a meaningful title and description summarizing the change.
- Merge the PR into `main`.
